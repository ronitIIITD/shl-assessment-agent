# Path to the built catalog. On Render/Railway this can stay as data/catalog.json
CATALOG_PATH=data/catalog.json

# Optional. The implementation works without an LLM. Add this only if you want LLM reranking later.
GROQ_API_KEY=
